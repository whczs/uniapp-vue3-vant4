// 全局配置文件
const baseUrl = '';
const staticUrl = '';
const fileUrl = ''; // 静态资源路径
const mapKey = '';

// 版本号 用于更新
const version = 1;
// vuex_version版本号 用于显示
const vuex_version = '1.0.' + version;
// 是否需要热更新（后台自动更新）
const flag_hot = false;

export default {
  baseUrl,
  staticUrl,
  fileUrl,
  version,
  vuex_version,
  mapKey,
}

